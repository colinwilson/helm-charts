Connect to the target by using kubectl port-forward to forward one of your local ports to a redis-proxy pod's port {{ .Values.source.address.port }}, or to the service using svc/{{ include "redis-proxy.fullname" . }}.